name = "capitols-flight-price"
main = "flight-price.js"
compatibility_date = "2024-11-01"

# Non-secret vars (edit as you like):
[vars]
# Lock this to your site origin in production, e.g. "https://louisnot.github.io"
ALLOW_ORIGIN = "*"
# Amadeus host. Use the test host while trying the sandbox:
# AMADEUS_BASE = "https://test.api.amadeus.com"
AMADEUS_BASE = "https://api.amadeus.com"

# Secrets are NOT stored here. Set them once with:
#   npx wrangler secret put AMADEUS_CLIENT_ID
#   npx wrangler secret put AMADEUS_CLIENT_SECRET
